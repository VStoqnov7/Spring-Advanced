# Използваме официален образ на OpenJDK за база
FROM openjdk:11-jre-slim

# Дефинираме работната директория в контейнера
WORKDIR /app

# Копираме JAR файла от target директорията в контейнера
COPY target/P01MobiLeLeLe.jar /app/app.jar

# Излагаме порт 8080, на който работи Spring Boot приложението
EXPOSE 8080

# Дефинираме команда за стартиране на приложението при стартиране на контейнера
CMD ["java", "-jar", "app.jar"]